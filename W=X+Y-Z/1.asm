;当W>10时
DATAS SEGMENT  
    X DB ?
    SHANG DB ?
    YU DB ?
DATAS ENDS

STACKS SEGMENT

STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX

	MOV AH,1
	INT 21H
	MOV X,AL 
	
	MOV AH,1
	INT 21H
	ADD X,AL
	
	MOV AH,1
	INT 21H   
	SUB X,AL 
	MOV AL,X
	SUB AL,30H
	
	MOV AH,0
    	MOV BL,10
	DIV BL
	MOV SHANG,AL
	MOV YU,AH
	
	MOV AH,2
	MOV DL,0AH
	INT 21H
	
	MOV AH,2
	ADD SHANG,30H
	MOV DL,SHANG
	INT 21H
	
	MOV AH,2
	ADD YU,30H
	MOV DL,YU
	INT 21H

    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
