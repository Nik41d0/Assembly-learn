;W<0
DATAS SEGMENT  
    X DB ?
    SHANG DB ?
    YU DB ?
DATAS ENDS

STACKS SEGMENT

STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX

	MOV AH,1
	INT 21H
	MOV X,AL 
	
	MOV AH,1
	INT 21H
	ADD X,AL
	
	MOV AH,1
	INT 21H   
	SUB X,AL
	SUB X,30H
	NEG X 
	MOV AL,X
	
	MOV AH,2
	MOV DL,0AH
	INT 21H
	
	MOV AH,2
	ADD SHANG,"-"
	MOV DL,SHANG
	INT 21H
	
	MOV AH,2
	ADD X,30H
	MOV DL,X
	INT 21H

    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
